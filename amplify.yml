# amplify.yml (in project root: infinos-user-admin/)
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd infinosfrontend-admin
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: infinosfrontend-admin/build
    files:
      - '**/*'
  cache:
    paths:
      - infinosfrontend-admin/node_modules/**/*